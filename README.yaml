---
#
# This is the canonical configuration for the `README.md`
# Run `make readme` to rebuild the `README.md`
#

# Name of this project
name: terraform-aws-crossaccountbucket

# Tags of this project
tags:
  - aws
  - terraform
  - terraform-modules
  - codepipeline

# Categories of this project
categories:
  - terraform-modules/CICD

# Logo for this project
#logo: docs/slalom-logo.png
# License of this project
license: "APACHE2"
# Copyrights
copyrights:
  - name: "Slalom, LLC"
    url: "https://slalom.com"
    year: "2019"
# Canonical GitHub repo
github_repo: jameswoolfenden/terraform-aws-crossaccountbucket

# Badges to display
badges:
  - name: "Build Status"
    image: "https://travis-ci.com/JamesWoolfenden/terraform-aws-crossaccountbucket.svg?branch=master"
    url: "https://travis-ci.com/JamesWoolfenden/terraform-aws-crossaccountbucket"
  - name: "Latest Release"
    image: "https://img.shields.io/github/release/JamesWoolfenden/terraform-aws-crossaccountbucket.svg"
    url: "https://github.com/JamesWoolfenden/terraform-aws-crossaccountbucket/releases/latest"
related:
  - name: "terraform-aws-codebuild"
    description: "Making a Build pipeline"
    url: "https://github.com/jameswoolfenden/terraform-aws-codebuild"

# Short description of this project
description: |-
  This module sets up an s3 buckets that is shared across aws accounts.
usage: |-
  It's a very simple module to use .
  The module file:

  ```make
  module "crossaccountbucket" {
    source  = "JamesWoolfenden/crossaccountbucket/aws"
    version = "0.1.6"
    name   = "${var.name}"
    aws_canonical_user_id = "${data.aws_canonical_user_id.prod.aws_canonical_user_id}"
    Secondary_account_id  = "${data.aws_caller_identity.prod.id}"
    }  ```

    You will need to provide 2 aws providers:

  ```hcl
  provider "aws" {
    region  = "eu-west-1"
    version = "2.0"
  }

  provider "aws" {
    region  = "eu-west-1"
    version = "2.0"
    alias   = "prod"
  }
  ```
  and supply the account and canonical id to the module. The example supplies and example resource.

include:
  - "docs/terraform.md"

# Contributors to this project
contributors:
  - name: "James Woolfenden"
    github: "jameswoolfenden"
